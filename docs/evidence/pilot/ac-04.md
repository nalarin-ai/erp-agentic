# MVP-AC-04 — required ambiguity blocks posting

- Criteria: missing/ambiguous input blocks the workflow with zero official number and zero provider writes.

## How the tests prove it

Suite: `tests/e2e/pilot/test_ac_04_ambiguity.py` (5 tests; every denial asserts **zero provider writes** by inspecting the fixture adapter state after the call).

1. No-lines preview → blocked (zero provider writes).
2. Multi-unit actor without an explicit unit selection → denied `UNIT_CONTEXT_REQUIRED` (zero provider writes).
3. Unverified actor identity → denied (zero provider writes).
4. Cross-unit selection (actor not assigned) → denied (zero provider writes).
5. Post attempt on a blocked draft → no official number issued, no provider write.

## Actual run

```
Ran 5 tests — OK (2026-08-15T01:59:25Z)
```
