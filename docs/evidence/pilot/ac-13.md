# MVP-AC-13 — distinct unit logo/template and immutable posted branding snapshot

- Criteria: each unit renders with its own logo/template; the posted invoice freezes an immutable branding snapshot; wrong unit asset, financial-identity override, or historical rewrite fails.

## How the tests prove it

Suite: `tests/e2e/pilot/test_ac_13_branding.py` (4 tests, real UNIT-001 versioned settings + FLOW-001 preview + FLOW-002 post).

1. Positive: two units produce distinct branding (logo/template reference) at preview time.
2. Positive: posted invoice freezes the branding snapshot (later settings change does not rewrite history).
3. Negative: preview/post with another unit's branding asset → denied.
4. Negative: attempt to override the financial identity inside branding/template fields → denied (policy-owned, not template-owned).

## Actual run

```
Ran 4 tests — OK (2026-08-15T01:59:25Z)
```
