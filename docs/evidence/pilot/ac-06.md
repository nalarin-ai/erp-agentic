# MVP-AC-06 — unauthorized sensitive actions denied

- Criteria: unauthorized sensitive actions are denied with zero disclosure and zero mutation.

## How the tests prove it

Suite: `tests/e2e/pilot/test_ac_06_unauthorized.py` (7 tests, real FND-002 authz + ISOFIX-001 policy boundary).

1. Sales role attempting a finance/post action → denied, no mutation.
2. Sales role attempting unit settings change → denied, no mutation.
3. Unassigned actor touching a unit → denied, zero disclosure (generic message, no existence leak).
4. Expired/revoked assignment → denied (stale context not honored).
5. Owner-only roll-up surface requested by a unit actor → denied.
6. Denial paths emit audit events (durable audit on denial, not only on success).
7. Denial messages are generic static strings — no reason/role/username interpolation.

## Actual run

```
Ran 7 tests — OK (2026-08-15T01:59:25Z)
```
