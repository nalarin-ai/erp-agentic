# MVP-AC-02 — Heavy Equipment → Contractor shared account

- Criteria: Heavy Equipment and Contractor units share the designated account (ACC-CONTRACTOR); any other account is denied.

## How the tests prove it

Suite: `tests/e2e/pilot/test_ac_02_shared_account.py` (5 tests, real UNIT-001 settings + FND-003 financial-identity policy + FLOW-002 post path via `PilotHarness`).

1. Positive: HE invoice resolves the shared ACC-CONTRACTOR account.
2. Positive: Contractor invoice resolves the same shared account (shared, not duplicated).
3. Negative: resolution to any other account alias → denied (`OVERRIDE_AUTHORIZATION_REQUIRED`).
4. Negative/freeze: posted HE invoice freezes the ACC-CONTRACTOR binding (immutable branding/account snapshot — historical rewrite fails).
5. Negative: HE ↔ Contractor pipeline isolation stays intact despite the shared account (no cross-pipeline read).

## Actual run

```
Ran 5 tests — OK (2026-08-15T01:59:25Z)
```
