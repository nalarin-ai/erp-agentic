# MVP-AC-01 — Banyumedia + Contractor and denied cross-sales

- Criteria (TRACEABILITY_MATRIX.md): Banyumedia and Contractor units configured and operable; competing sales actors cannot read/claim each other's pipelines; native/gateway leak on final architecture fails.
- Task proof chain: UNIT-001 (units), CRM-001 (gateway CRM port scoping), ISOFIX-001 (final gateway-only isolation policy).

## How the tests prove it

Suite: `tests/e2e/pilot/test_ac_01_cross_sales.py` (8 tests, `PilotHarness` wires the real production components — UNIT-001 registry/settings, FND-002 authz, FND-003 policy, FLOW-001/002 workflows, CRM-001 port contract, ISOFIX-001 `src/isolation_architecture/policy.py` — over network-disabled fixture adapters; synthetic opaque refs only; deterministic clock).

1. Positive: Banyumedia sales creates and reads own lead (gateway allows in-unit flow).
2. Positive: Contractor sales creates and reads own lead.
3. Negative: cross-unit lead read via gateway CRM port → denied fail-closed.
4. Negative: cross-unit search returns zero foreign rows.
5. Negative: native surfaces (NATIVE_API / NATIVE_DESK / etc.) DENIED for unit-scoped roles by the final isolation policy (total decision matrix).
6. Positive: gateway surfaces ALLOWED for unit-scoped roles on the same policy.
7. Negative: native credential issuance to unit-scoped roles denied with a generic static message (no reason/username echo).
8. Negative: unknown/hostile role classification denied fail-closed.

## Actual run

```
python3 -m unittest discover -s tests/e2e/pilot -t . -p test_ac_01_cross_sales.py
Ran 8 tests — OK (2026-08-15T01:59:25Z)
```

Full suite on the same tree: 817 tests = 2 pre-existing state-dependent fixture failures (CRM export + payment evidence index, outside this task) + 11 by-design skips, 0 errors.
